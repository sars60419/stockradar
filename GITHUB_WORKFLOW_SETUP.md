# GitHub Workflow 建立教學

因為 `.github` 是隱藏資料夾，GitHub 網頁上傳常常會擋住。請用這個方法。

1. 到 Repository。
2. Add file → Create new file。
3. 檔名輸入：

```text
.github/workflows/update.yml
```

4. 把 `workflow_update.yml` 內容全部貼上。
5. Commit changes。

完成後，到 Actions 會看到 `Update StockRadar Data`。
